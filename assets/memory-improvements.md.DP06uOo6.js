import{_ as t,a as o,o as a,Y as r}from"./chunks/framework.BFAekxW2.js";const f=JSON.parse('{"title":"Memory & SD-uptime improvements","description":"","frontmatter":{},"headers":[],"relativePath":"memory-improvements.md","filePath":"memory-improvements.md"}'),i={name:"memory-improvements.md"};function d(s,e,c,n,l,h){return a(),o("div",null,e[0]||(e[0]=[r('<h1 id="memory-sd-uptime-improvements" tabindex="-1">Memory &amp; SD-uptime improvements <a class="header-anchor" href="#memory-sd-uptime-improvements" aria-label="Permalink to “Memory &amp; SD-uptime improvements”">​</a></h1><p>Goal: minimize SD card up-time and make the best use of the 8 MB PSRAM chip, iPod-style — preload aggressively in big bursts, then let the SD card go idle for as long as possible, instead of the current continuous small-chunk trickle. Background/analysis: see the audio-stream review in this conversation; the findings below are ordered so each step is safe to ship on its own.</p><h2 id="status" tabindex="-1">Status <a class="header-anchor" href="#status" aria-label="Permalink to “Status”">​</a></h2><p>§0-§4 are implemented in code. §5 (hardware SD power-switch) is intentionally left as a future, separate hardware track. Files touched:</p><ul><li><code>software/src/storage/PSRAM_controller.hpp</code> — fixed <code>PSRAM_SIZE</code>, added <code>NEXT_TRACK_BUFFER_BASE_ADDRESS</code>/<code>NEXT_TRACK_BUFFER_SIZE</code>, added static_asserts.</li><li><code>software/src/sound/Audio_buffer.h</code> / <code>.cpp</code> — hysteresis watermarks, chunk-capped <code>load()</code>, <code>getBufferedBytes()</code>, next-track prefetch API (<code>prepareNextTrack</code>/<code>prefetchNextTrack</code>/<code>consumeNextTrackIfMatches</code>/ <code>invalidateNextTrack</code>), shared <code>_scratchBuffer</code>.</li><li><code>software/src/sound/AudioPlayer.h</code> / <code>.cpp</code> — <code>loop()</code> now gates refills on the low watermark and prefetches the next track otherwise; <code>startPlaying()</code> tries to adopt a matching prefetch before falling back to a fresh SD open; thin <code>prepareNextTrack</code>/<code>invalidateNextTrack</code> wrappers.</li><li><code>software/src/state/state.h</code> / <code>.cpp</code> — new <code>State::getNextTrackId()</code>, reusing the same <code>resolveTrackId</code> lookup <code>notifyTrackEnded</code> already used reactively, but callable proactively.</li><li><code>software/src/sound/player.cpp</code> — <code>handleTrackPlaybackStarted</code> now also resolves and prepares the next track&#39;s prefetch (or invalidates it at end of list) right after starting the current one.</li></ul><p>Not done here (flagged, not blocking): the §0 audit of the <em>actual</em> PSRAM footprint of a real, fully-populated <code>Music_index</code> wasn&#39;t performed — no runtime bound currently exists on it (see note in §0 below), so this is worth checking against a real library before shipping.</p><h2 id="_0-fix-the-psram-size-bug-first-correctness-blocks-everything-else" tabindex="-1">0. Fix the PSRAM_SIZE bug first (correctness, blocks everything else) <a class="header-anchor" href="#_0-fix-the-psram-size-bug-first-correctness-blocks-everything-else" aria-label="Permalink to “0. Fix the PSRAM_SIZE bug first (correctness, blocks everything else)”">​</a></h2><p><code>software/src/storage/PSRAM_controller.hpp:6</code>:</p><div class="language-cpp"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#define</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> PSRAM_SIZE</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (64 * 1024 * 1024)</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> // 64 Mb PSRAM chip size</span></span></code></pre></div><p>The APS6404L is a 64-<strong>Megabit</strong> chip = <strong>8 MB</strong>, not 64 MB — <code>getCapacity()</code> in the same file already returns the correct <code>8 * 1024 * 1024</code>. But <code>MUSIC_INDEX_SIZE = PSRAM_SIZE - AUDIO_BUFFER_SIZE</code> uses the wrong constant, so the music index thinks it owns ~58 MB of address space when only ~2 MB physically exists past the 6 MB audio buffer. Any address past that real boundary wraps around the physical 8 MB chip and silently corrupts the audio ring buffer at address 0 — exactly the failure mode already called out in the comment in <code>Audio_buffer.cpp</code> (<code>initializePSRAM()</code>) about the two regions overlapping.</p><ul><li>[x] Change <code>PSRAM_SIZE</code> to <code>(8 * 1024 * 1024)</code>.</li><li>[x] Add a compile-time assertion (<code>AUDIO_BUFFER_SIZE + NEXT_TRACK_BUFFER_SIZE &lt; PSRAM_SIZE</code> in <code>PSRAM_controller.hpp</code>, plus <code>NEXT_TRACK_BUFFER_SIZE &lt;= AUDIO_BUFFER_SIZE</code> in <code>Audio_buffer.h</code> guarding the no-wrap assumption in <code>consumeNextTrackIfMatches()</code>), so this class of bug fails to compile instead of silently corrupting audio.</li><li>[ ] <strong>Still open</strong>: audit <code>Music_index</code>/<code>Music_lookup</code> for how large the on-PSRAM index actually gets with a full library. <code>MUSIC_INDEX_SIZE</code> is a documented budget, not an enforced bound — nothing in <code>Music_index.cpp</code> currently checks a write against it, so a large enough real library&#39;s index (which appears to be loaded fully into PSRAM at <code>MUSIC_INDEX_BASE_ADDRESS</code>, not streamed/paged from SD) could still silently overrun into the next-track prefetch/audio regions. Confirm it fits the real 2 MB remaining, or shrink <code>AUDIO_BUFFER_SIZE</code>/ <code>NEXT_TRACK_BUFFER_SIZE</code> if it doesn&#39;t.</li></ul><p>Do this before touching the buffering logic below: making the ring buffer bigger/fuller (§2) makes this bug more likely to actually manifest.</p><h2 id="_1-add-hysteresis-to-the-refill-logic-the-core-behavior-change" tabindex="-1">1. Add hysteresis to the refill logic (the core behavior change) <a class="header-anchor" href="#_1-add-hysteresis-to-the-refill-logic-the-core-behavior-change" aria-label="Permalink to “1. Add hysteresis to the refill logic (the core behavior change)”">​</a></h2><p>Today, <code>Audio_buffer::load()</code> (<code>Audio_buffer.cpp:194</code>) refills the instant the buffer drops below <code>AUDIO_TARGET_BUFFERED_BYTES</code> (256 KB), and <code>AudioPlayer::loop()</code> calls <code>load()</code> on <em>every</em> main-loop tick. Net effect: the SD card is touched in small 4 KB bursts (<code>AUDIO_PRELOAD_CHUNK_SIZE</code>) continuously through playback — never idle for more than a fraction of a second.</p><ul><li>[x] Added a second, lower watermark, <code>AUDIO_LOW_WATERMARK_BYTES</code> (<code>AUDIO_TARGET_BUFFERED_BYTES / 8</code>, ~12.5%) in <code>Audio_buffer.h</code>.</li><li>[x] <code>AudioPlayer::loop()</code> now only calls <code>_buffer.load()</code> when <code>_buffer.getBufferedBytes() &lt; AUDIO_LOW_WATERMARK_BYTES</code>; otherwise it spends that tick on <code>_buffer.prefetchNextTrack()</code> (§3) instead of touching the SD card at all.</li><li>[x] Kept the existing seek-time bounded refill path (<code>AUDIO_SEEK_PRIME_BYTES</code>, <code>AudioPlayer::seekToSeconds</code>) untouched.</li><li>[x] <strong>Correction to this plan&#39;s original assumption</strong>: simply raising the target did <em>not</em> automatically stay non-blocking - <code>load()</code> used to loop internally until the target was reached in one call, so a 4.5 MB target would have blocked the caller for up to ~1152 sequential 4 KB SD reads in a single call. Fixed by adding <code>AUDIO_MAX_CHUNKS_PER_LOAD_CALL</code> (4): <code>load()</code> now does at most 4 SD reads per call and relies on being re-invoked next tick to keep making progress, so a big refill still spreads across many ticks instead of stalling the UI once.</li></ul><h2 id="_2-raise-the-buffered-target-to-actually-use-the-reserved-6-mb" tabindex="-1">2. Raise the buffered target to actually use the reserved 6 MB <a class="header-anchor" href="#_2-raise-the-buffered-target-to-actually-use-the-reserved-6-mb" aria-label="Permalink to “2. Raise the buffered target to actually use the reserved 6 MB”">​</a></h2><ul><li>[x] Raised <code>AUDIO_TARGET_BUFFERED_BYTES</code> to 4.5 MB, and resized <code>AUDIO_BUFFER_SIZE</code> itself from 6 MB to 5 MB in <code>PSRAM_controller.hpp</code> to make room for the new 1 MB <code>NEXT_TRACK_BUFFER_SIZE</code> region (§3) without growing past the physical 8 MB chip - <code>MUSIC_INDEX_BASE_ADDRESS</code> ends up at the same 6 MB offset as before, so the music index&#39;s actual location in PSRAM is unaffected.</li><li>[ ] <strong>Not verified on hardware</strong> (no device available in this environment): confirm on real hardware that a 128 kbps MP3 actually coasts for several minutes between refills as expected, and that FLAC/high-bitrate files still behave reasonably (proportionally less idle time is expected, not a bug).</li><li>[ ] <strong>Not addressed</strong>: <code>seekToSeconds</code>/<code>seekBySeconds</code> still call <code>resetRingBuffer()</code> unconditionally, discarding the whole (now much bigger, up to 4.5 MB) preloaded buffer on every seek. Still considered acceptable (seeks are user-driven and rare relative to playback time), but this got bigger with this change and doesn&#39;t yet have a comment calling out the trade-off at the call site - worth adding if seeking ever feels wasteful in practice.</li></ul><h2 id="_3-prefetch-the-next-track-ahead-of-time" tabindex="-1">3. Prefetch the next track ahead of time <a class="header-anchor" href="#_3-prefetch-the-next-track-ahead-of-time" aria-label="Permalink to “3. Prefetch the next track ahead of time”">​</a></h2><p><code>State::notifyTrackEnded</code> (<code>software/src/state/state.cpp:81</code>) only resolves and starts the next track <em>after</em> the current one fully ends, even though sequential next-track-id is knowable well before that point. Today every track change costs a fresh burst of SD activity right at the transition.</p><ul><li>[x] <code>State::getNextTrackId()</code> (new, reuses the same <code>resolveTrackId</code> lookup <code>notifyTrackEnded</code> already used reactively) resolves the sequential next track proactively. <code>PodPlayer::handleTrackPlaybackStarted</code> calls it right after starting the current track and hands the result to <code>AudioPlayer::prepareNextTrack()</code>.</li><li>[x] <code>AudioPlayer::loop()</code> calls <code>_buffer.prefetchNextTrack()</code> (one bounded <code>AUDIO_PRELOAD_CHUNK_SIZE</code> chunk per call) only on ticks where the current-track buffer is already healthy (above the low watermark), so it never competes with the current track&#39;s own refill for the SPI bus.</li><li>[x] Sized the next-track preload at <code>NEXT_TRACK_BUFFER_SIZE</code> = 1 MB (linear region, not a ring - filled once from byte 0) - enough to smooth a transition, not a second full song buffer (8 MB budget, see §4).</li><li>[x] Hand-off implemented as <code>Audio_buffer::consumeNextTrackIfMatches()</code>, called from <code>AudioPlayer::startPlaying()</code> before falling back to a fresh <code>setFileName()</code>: if the requested filename matches what was prefetched, it reopens that file at the resumed position (cheap - a directory lookup + seek, not a data re-read) and copies the already-prefetched bytes into the now-empty main ring buffer, so that portion needs no SD read at all.</li><li>[x] Invalidation: <code>consumeNextTrackIfMatches()</code> always clears the prefetch slot (whether it matched or not), and <code>PodPlayer::handleTrackPlaybackStarted</code> calls <code>AudioPlayer::invalidateNextTrack()</code> when there&#39;s no next track (end of list) so a stale guess doesn&#39;t linger. Note: <code>AudioPlayer::forward()</code>/ <code>rewind()</code> are still restart-the-current-file stubs (per their existing comments), not real track-skip - real track changes go through <code>State::startPlayback</code>/<code>EVENT_PLAYBACK_STARTED</code>, which is what&#39;s wired up here. If/when <code>forward()</code>/<code>rewind()</code> become real skip actions, wire <code>invalidateNextTrack()</code> there too if the new target doesn&#39;t match the current guess.</li></ul><h2 id="_4-psram-memory-budget-—-decide-the-split-up-front" tabindex="-1">4. PSRAM memory budget — decide the split up front <a class="header-anchor" href="#_4-psram-memory-budget-—-decide-the-split-up-front" aria-label="Permalink to “4. PSRAM memory budget — decide the split up front”">​</a></h2><p>Total PSRAM: 8 MB. After step 0&#39;s fix, budget explicitly instead of leaving it implicit:</p><ul><li>[x] Split decided and encoded directly as the macros in <code>PSRAM_controller.hpp</code>: 5 MB current-track buffer (<code>AUDIO_BUFFER_SIZE</code>) + 1 MB next-track prefetch (<code>NEXT_TRACK_BUFFER_SIZE</code>) + 2 MB music index (<code>MUSIC_INDEX_SIZE</code>, now correctly computed - see §0&#39;s still-open audit item for whether a real library&#39;s index actually fits that).</li><li>[x] Expectations documented (here and in the macro comments): this is enough for roughly one song fully resident plus a prefetched head-start on the next one — not &quot;many songs&quot; simultaneously resident like a RAM-heavy iPod. Getting closer to that would need a bigger PSRAM chip in a future hardware revision; not in scope here.</li></ul><h2 id="_5-optional-hardware-true-sd-power-down" tabindex="-1">5. (Optional, hardware) True SD power-down <a class="header-anchor" href="#_5-optional-hardware-true-sd-power-down" aria-label="Permalink to “5. (Optional, hardware) True SD power-down”">​</a></h2><p><code>pinout.h</code> only defines <code>CARDCS</code> (chip select) for the SD card — there&#39;s no power-switch/load-switch GPIO on its supply rail. Software alone can only stop clocking and deselect the card (SD spec&#39;s standby idle current — real savings, but not zero).</p><ul><li>[ ] If chasing further power savings later: add a small P-MOSFET or load switch on the SD socket&#39;s 3.3V rail, gated by a spare GPIO, so firmware can fully cut SD power during the long idle windows §1-§3 create, and re-enable + re-<code>begin()</code> the SD card right before the next scheduled burst refill.</li><li>[ ] This is a hardware change (new component + PCB net), so track it separately from the firmware work above; not a blocker for §0-§4.</li></ul><h2 id="suggested-order-of-work" tabindex="-1">Suggested order of work <a class="header-anchor" href="#suggested-order-of-work" aria-label="Permalink to “Suggested order of work”">​</a></h2><ol><li>§0 (bug fix) — safety, low effort, no behavior change to buffering itself. <strong>Done.</strong></li><li>§1 + §2 together (hysteresis + bigger target) — the actual &quot;idle for minutes instead of seconds&quot; win, moderate effort, all in <code>Audio_buffer</code>. <strong>Done.</strong></li><li>§3 (next-track prefetch) — bigger change, touches <code>AudioPlayer</code>/<code>state.cpp</code> interaction; do after §1/§2 are validated stable. <strong>Done.</strong></li><li>§4 — just documentation/constants once §0-§3 land. <strong>Done.</strong></li><li>§5 — separate hardware track, whenever a board revision is on the table. <strong>Not started (hardware).</strong></li></ol><h2 id="remaining-before-calling-this-done" tabindex="-1">Remaining before calling this done <a class="header-anchor" href="#remaining-before-calling-this-done" aria-label="Permalink to “Remaining before calling this done”">​</a></h2><ul><li><strong>No hardware in this environment to test on.</strong> All of the above compiles by inspection (matched against existing SdFat/PSRAM usage patterns elsewhere in the file) but hasn&#39;t been built or run on the actual STM32 + VS1053 + APS6404L hardware. Flash it and verify: normal playback, seeking, track-end transitions (both prefetched-and-matching and end-of-list), and that the SD card genuinely goes idle for extended stretches (e.g. probe <code>CARDCS</code>/SPI activity, or log timestamps around <code>SDtoPSRAM()</code> calls).</li><li>The <code>Music_index</code> real-world size audit flagged in §0 - do this before trusting the 2 MB budget on a large library.</li><li><code>test/</code> only compiles <code>storage/*.cpp</code> for the native/doctest environment (<code>platformio.ini</code>&#39;s <code>test_native</code> env) - none of this sound/ or state/ code has automated test coverage either before or after this change.</li></ul>',30)]))}const p=t(i,[["render",d]]);export{f as __pageData,p as default};
